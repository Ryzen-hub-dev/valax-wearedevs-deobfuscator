const {
  IsolatedRunner,
  DEFAULT_TIMEOUT_MS,
  DEFAULT_MAX_OUTPUT_BYTES,
  ALLOWED_ENV_VARS
} = require('./isolated-runner');

module.exports = {
  IsolatedRunner,
  DEFAULT_TIMEOUT_MS,
  DEFAULT_MAX_OUTPUT_BYTES,
  ALLOWED_ENV_VARS
};
